# Benchmarks

Planned acceptance metrics: prefill tokens/s, decode tokens/s, peak RSS, first-token latency, and parity against the reference NedoLM runtime. Benchmarks must record CPU model, ISA, thread count, quantization, OS and compiler.
